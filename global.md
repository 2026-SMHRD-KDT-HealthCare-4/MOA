# MOA Global Development Guide

이 문서는 MOA 프로젝트의 최상위 기준 문서다. 새로 합류한 개발자나 AI 에이전트는 코드를 수정하기 전에 이 파일을 먼저 읽고, 여기서 연결된 세부 문서를 따라 현재 상태를 파악한다.

## 1. 프로젝트 한 줄 정의

MOA는 고령층이 매일 음성으로 건강 상태를 기록하고, 보호자가 변화 흐름을 확인하는 웰니스 서비스다. 의료기기나 진단 서비스가 아니며, UI와 응답 문구는 항상 "참고", "변화", "패턴", "기록" 중심으로 표현한다.

## 2. 문서 구조

| 문서 | 목적 |
| --- | --- |
| `global.md` | 최상위 인덱스, 현재 개발 상태, 문서 갱신 규칙 |
| `docs/01_product_planning.md` | 제품 기획, 사용자, MVP 범위, Out of Scope |
| `docs/02_common_development.md` | 공통 개발 원칙, 레포 구조, 상태/데이터/API 설계 기준 |
| `docs/03_frontend.md` | 프론트엔드 구조, 화면, 라우팅, 현재 구현 상태 |
| `docs/04_backend_ml.md` | 백엔드와 ML의 목표 구조, 아직 미구현인 부분, API 방향 |
| `docs/features/elder_app.md` | 고령자 앱 기능 상세 |
| `docs/features/guardian_app.md` | 보호자 앱 기능 상세 |
| `docs/features/voice_chat_record.md` | 녹음, STT, 챗봇, TTS, 호출어 기능 상세 |
| `docs/policies/safety_privacy.md` | 안전 표현, 개인정보, 음성 데이터 처리 정책 |
| `frontend/CLAUDE.md` | 프론트 작업 시 지켜야 할 로컬 규칙 |
| `frontend/docs/` | 기존 프론트 MVP/온보딩 문서. 신규 기준은 최상위 `docs/`를 우선한다. |

## 3. 현재 개발 상태

### 완료 또는 프로토타입 구현

- Expo Router 기반 프론트 앱 구조가 잡혀 있다.
- 인트로, 로그인, 회원가입, 고령자 탭, 보호자 탭이 있다.
- 고령자 앱에는 홈, 녹음, 기록 히스토리, 설정 화면이 있다.
- 보호자 앱에는 대시보드, 가족 연결, 리포트, 설정 화면이 있다.
- 녹음은 `expo-av` 기반이며, OpenAI Whisper API 호출 코드와 mock transcript fallback이 있다.
- 챗봇은 mock 응답을 사용하며, OpenAI TTS API 호출 코드와 실패 시 텍스트 유지 fallback이 있다.
- 웹에서는 루트 레이아웃이 430px 모바일 프레임으로 표시되도록 조정되어 있다.
- `npm.cmd run typecheck` 기준 TypeScript 체크는 통과한다.
- **FCM 푸시 알림 및 스케줄러**:
  - `firebase-admin` 기반 FCM 발송 유틸리티 및 로컬 개발용 Mock Fallback Mode를 구현함.
  - 디바이스 토큰 등록을 위한 `POST /auth/fcm-token` API 추가.
  - `apscheduler` 기반 백그라운드 스케줄러를 탑재하여 복약 시간 알림(매 분 체크) 및 병원 방문 알림(하루 전 12:00, 당일 07:00) 발송을 처리함.
  - 병원 방문 일정 관리를 위한 `hospital_visit` 테이블 및 CRUD API (`/hospital`) 추가.
  - 4대 질환(파킨슨/치매/우울/당뇨) 중 하나라도 `AMBER`('경보')인 날이 3일 연속 또는 7일 중 4회 이상 발생 시 보호자 알림을 자동 전송하는 누적 트리거 로직을 구현함.

### 아직 미구현 또는 mock 상태

- 백엔드 폴더는 현재 `.gitkeep`만 있다. (FastAPI 골격 외에 Supabase 등 실 서비스 연동은 진행 중)
- ML 폴더도 현재 `.gitkeep`만 있다.
- 로그인/회원가입은 실제 인증 API와 연결되어 있지 않다.
- 기록 저장, 보호자 리포트, 가족 연결은 mock 데이터 기반이다.
- OpenAI API 키가 클라이언트 환경 변수에 의존하는 구조라, 실서비스에서는 서버로 이전해야 한다.
- 실제 백그라운드 호출어 감지는 Out of Scope이며, 현재는 hook과 store 수준의 mock/연결 구조만 있다.

## 4. 개발 우선순위

1. 문서와 코드의 용어, 정책, MVP 범위를 일치시킨다.
2. 프론트 mock 화면의 안정성을 유지한다.
3. mock 데이터 타입을 실제 API 계약으로 승격할 수 있게 정리한다.
4. OpenAI STT/TTS 호출은 백엔드 API로 이전한다.
5. 백엔드가 생기면 프론트는 직접 API 키를 다루지 않는다.
6. ML은 먼저 입력/출력 스키마와 안전한 표현 정책을 정의한 뒤 모델 구현으로 들어간다.

## 5. 반드시 지켜야 할 정책

- "진단", "처방", "치료", 질병명 단정 표현을 쓰지 않는다.
- 위험/경고 상황에서도 빨간색을 쓰지 않는다. 경고 계열은 `#E8943A`를 사용한다.
- 고령자 화면은 최소 18pt 텍스트와 56px 이상의 터치 영역을 기본으로 한다.
- 고령자 화면에서 수치, 퍼센트, 점수를 직접 노출하지 않는다.
- 보호자 화면에서는 추이 그래프와 요약을 허용하되 의료적 판단처럼 보이면 안 된다.
- 임시 음성 파일은 분석 또는 전송이 끝나면 즉시 삭제한다.
- 콘솔 로그에 발화 텍스트, 사용자 식별 정보, 가족 정보, 음성 URI를 남기지 않는다.
- Out of Scope 항목은 사용자가 명시적으로 범위 변경을 승인하기 전까지 구현하지 않는다.

상세 정책은 `docs/policies/safety_privacy.md`를 따른다.

## 6. 문서 갱신 규칙

개발자는 기능을 추가하거나 구조를 바꿀 때 반드시 문서를 함께 갱신한다. 이 규칙은 사람 개발자와 AI 에이전트 모두에게 적용된다.

### 언제 갱신해야 하는가

- 새 화면, 라우트, API, store, hook, 데이터 모델을 추가했을 때
- mock을 실제 API로 교체했을 때
- MVP 범위 또는 Out of Scope가 바뀌었을 때
- 사용자에게 보이는 문구 정책이 바뀌었을 때
- 개인정보, 음성 파일, 인증, 보호자 공유 정책이 바뀌었을 때
- 기존 문서와 다른 구현 결정을 했을 때

### 어디를 갱신해야 하는가

- 제품 범위 변경: `docs/01_product_planning.md`
- 공통 구조/개발 규칙 변경: `docs/02_common_development.md`
- 프론트 구현 변경: `docs/03_frontend.md`
- 백엔드/ML/API 변경: `docs/04_backend_ml.md`
- 고령자 기능 변경: `docs/features/elder_app.md`
- 보호자 기능 변경: `docs/features/guardian_app.md`
- 녹음/STT/TTS/챗봇/호출어 변경: `docs/features/voice_chat_record.md`
- 안전/개인정보 정책 변경: `docs/policies/safety_privacy.md`
- 전체 상태나 우선순위 변경: 이 `global.md`

### 갱신 방식

1. 코드 변경과 같은 작업 단위에서 문서를 수정한다.
2. 문서에는 "현재 구현됨", "mock", "예정", "금지"를 명확히 구분한다.
3. 추측을 사실처럼 쓰지 않는다. 아직 구현되지 않은 내용은 "예정" 또는 "권장"으로 표시한다.
4. 주요 변경은 각 문서 하단의 변경 기록에 날짜와 요약을 남긴다.
5. 문서와 코드가 충돌하면 작업을 멈추고 먼저 충돌을 해소한다.

## 7. 변경 기록

- 2026-06-24: FCM 푸시 알림 및 백그라운드 스케줄러(복약/병원 방문), 병원 방문 일정 테이블/API 신설, AMBER 경보 누적 조건 알림 로직 구현.
- 2026-06-15: 최상위 문서 체계 신설. 제품, 공통 개발, 프론트, 백엔드/ML, 기능별 스펙, 정책 문서로 분리.
