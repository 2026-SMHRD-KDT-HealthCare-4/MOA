# 팀 공유용 예시. 실제 사용 시 이 파일을 `.env.local` 로 복사해서 값을 채운다.
# (.env.local 은 .gitignore 처리되어 커밋되지 않으므로 각자 로컬에서만 적용된다.)

# DEV 전용 자동 로그인: 로그인/클레임을 건너뛰고 해당 역할로 바로 부팅한다.
#   elder    → 직접사용자(챗봇 메인 화면)
#   guardian → 보호자
# 비워두거나 줄을 지우면 정상 인증 흐름. 값 변경 후 expo dev 서버 재시작 필요.
EXPO_PUBLIC_DEV_AUTOLOGIN=
